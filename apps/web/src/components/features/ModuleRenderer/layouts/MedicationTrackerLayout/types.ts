// Métadonnées extraites des fields pour l'aperçu (zéro texte en dur, tout vient
// de la config + i18n). Pastilles neutres — aucune gravité clinique encodée (MDR).
export interface PreviewStatus {
  value: string
  label: string
  color: string
  bgColor: string
}

export interface PreviewReason {
  value: string
  label: string
}
