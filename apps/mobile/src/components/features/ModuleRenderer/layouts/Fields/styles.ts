import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  fieldsBlock: { gap: 0 },
  infoBox: {
    flexDirection: 'row', gap: 6, alignItems: 'flex-start',
    marginTop: 12, padding: 10, backgroundColor: '#F3F4F6', borderRadius: 8,
  },
})
