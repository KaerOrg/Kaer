export type TimeRange = '7J' | '1M' | '6M' | '1A'

// DataPoint et XLabel vivent dans components/ui/Chart — re-exportés ici pour compatibilité interne.
export type { DataPoint, XLabel } from '../../../components/ui/Chart'

