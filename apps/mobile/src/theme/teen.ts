// Palette par défaut pour le mode ado. Les couleurs par module viennent de la BDD (modules.color).

export const TEEN_DEFAULT_COLOR = '#6366F1'

export const teenCardStyle = {
  borderLeftWidth: 4,
  borderRadius: 14,
} as const
